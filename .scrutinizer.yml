imports:
  - php

filter:
  excluded_paths: [vendor/*, test/*]

build:
  dependencies:
    before:
      - mkdir -p /home/scrutinizer/build/AsaTirsen/weather/cache/anax
      - chmod -R 777 /home/scrutinizer/build/AsaTirsen/weather/cache/anax
  nodes:
    analysis:
      tests:
        override:
          - php-scrutinizer-run

    tests-and-coverage:
      tests:
        override:
          -
            command: "make phpunit"
            coverage:
              file: "build/coverage.clover"
              format: "php-clover"

