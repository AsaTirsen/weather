imports:
  - php

filter:
  excluded_paths: [vendor/*, test/*]

build:

    dependencies:
      before:
        -
          - "mkdir build"
          - "chmod -R 777 /build/cache/anax"


    tests:
        override:
          -
            command: "make phpunit"
            coverage:
              file: "build/coverage.clover"
              format: "php-clover"

